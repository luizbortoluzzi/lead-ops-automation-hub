/** HTTP header carrying the machine-to-machine API key. */
export const API_KEY_HEADER = 'x-api-key';

/** Metadata key set by the {@link Public} decorator to bypass the API-key guard. */
export const IS_PUBLIC_KEY = 'isPublic';
