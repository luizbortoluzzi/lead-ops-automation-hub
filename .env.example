# ---------------------------------------------------------------------------
# LeadOps Automation Hub — local example values ONLY.
# Copy this file to `.env` and adjust as needed. Never commit a real `.env`.
# ---------------------------------------------------------------------------

NODE_ENV=development

# PostgreSQL (container)
POSTGRES_DB=leadops
POSTGRES_USER=leadops
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432

# Backend API
BACKEND_PORT=3000

# Frontend (React SPA served by nginx, reverse-proxies /api to the backend)
FRONTEND_PORT=5173

# How the backend connects to Postgres (host is the compose service name).
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=leadops
DATABASE_USER=leadops
DATABASE_PASSWORD=change-me

# Machine-to-machine auth between n8n and the backend (X-API-Key).
BACKEND_API_KEY=change-me-development-key

# n8n
N8N_PORT=5678
TZ=America/Sao_Paulo

# Mailpit (local e-mail capture)
MAILPIT_SMTP_PORT=1025
MAILPIT_WEB_PORT=8025
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_FROM=leadops@example.local
SALES_NOTIFICATION_EMAIL=sales@example.local
OPERATIONS_NOTIFICATION_EMAIL=operations@example.local

# Phase 3 — resilience (backend)
# Failure simulation is honored only in development/test, never in production.
ENABLE_FAILURE_SIMULATION=true
IDEMPOTENCY_RETENTION_DAYS=7
# "timeout" simulation delay; set above the n8n request timeout for realistic tests.
SIMULATED_TIMEOUT_DELAY_MS=6000

# Phase 3 — retry policy (configured MANUALLY inside the n8n WF06 workflow)
N8N_BACKEND_REQUEST_TIMEOUT_MS=5000
N8N_MAX_RETRY_ATTEMPTS=4
